<h1>Edition du profil</h1>

<div class="row">
  <div class="small-12 column">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', :f => f %>
      <div class="actions">
        <%= f.submit "Update" %>
      </div>
    <% end %>
  </div>
</div>

<div>
  <%= gravatar_for @user %>
  <a href="http://gravatar.com/emails">changer</a>
</div>
